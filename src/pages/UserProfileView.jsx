import { useState } from "react";

export default function UserProfileView({ currentUser, transactions, onSave, onCancel }) {
  const [username, setUsername] = useState(currentUser.username);
  const [pin, setPin] = useState('');
  const ownTransactions = transactions.filter(t => t.User === currentUser.username);
  return <div className="space-y-4"><div className="flex items-center justify-between"><button onClick={onCancel} className="text-gray-500"><i className="fa-solid fa-arrow-left text-lg"></i></button><h3 className="font-bold text-slate-800 text-base">User Profile</h3><div className="w-5"></div></div><form onSubmit={(e) => { e.preventDefault(); onSave({ username, pin }); }} className="bg-white rounded-2xl p-4 border border-gray-200 space-y-3"><div className="text-xs font-bold text-slate-700">নিজের তথ্য পরিবর্তন করুন</div><input value={username} onChange={(e) => setUsername(e.target.value)} placeholder="Username" className="w-full border border-gray-300 rounded-xl px-3 py-2 text-sm" required /><input type="password" value={pin} onChange={(e) => setPin(e.target.value)} placeholder="New PIN" className="w-full border border-gray-300 rounded-xl px-3 py-2 text-sm" required /><button className="w-full bg-emerald-600 text-white font-bold py-2.5 rounded-xl">Update Profile</button></form><div className="bg-white rounded-2xl p-4 border border-gray-200 space-y-2"><div className="text-xs font-bold text-slate-700">আমার যোগ করা লেনদেন ({ownTransactions.length})</div>{ownTransactions.length === 0 ? <div className="text-xs text-gray-400 py-4 text-center">কোনো লেনদেন নেই।</div> : ownTransactions.map(t => <div key={t.ID} className="flex justify-between border-b border-gray-100 py-2 text-xs"><span>{t.Description || t.SourceCategory || t.Type}<br /><span className="text-gray-400">{t.Date} • {t.Type}</span></span><strong className={t.Type === 'Expense' || t.Type === 'Transfer Out' ? 'text-rose-600' : 'text-emerald-600'}>{t.Currency} {Number(t.Amount || 0).toLocaleString('en-US', { minimumFractionDigits: 2 })}</strong></div>)}</div></div>;
}
